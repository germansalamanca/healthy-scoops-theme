<div class="container">
  <div class="row">
    <div class="col dt-sc-overlay-section" style="padding-bottom: 4rem;">
      <h1 style="font-size: 2.4rem">{{ page.title }}</h1>
      {{ page.content }}
    </div>
  </div>
</div>
